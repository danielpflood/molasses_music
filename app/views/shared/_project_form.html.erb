<%= form_for @project do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
<div class="field">
	<br>
	Project Name: <%= f.text_area :name %>
   Project Type: <%= f.text_area :project_type %>
  </div>
  <div class="actions">
    <%= f.submit "Submit" %>
  </div>
<% end %>
